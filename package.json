{
  "name": "dc-hotspots",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "postinstall": "cp node_modules/maplibre-gl/dist/maplibre-gl-worker.mjs node_modules/maplibre-gl/dist/maplibre-gl-shared.mjs public/",
    "deploy": "npm run build && rm -rf dist/.git && cd dist && git init -q && git checkout -q -b gh-pages && git add -A && git commit -qm 'deploy' && git push -qf https://github.com/AviWise/out-dc.git gh-pages && cd .. && rm -rf dist/.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4",
    "maplibre-gl": "^6.6.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.0",
    "playwright-core": "^1.62.1",
    "postgres": "^3.4.9",
    "vite": "^8.2.2"
  }
}
